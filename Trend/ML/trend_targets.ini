[trend_ml.target.bull_2pct_4h]
# Example target for Trend Pillar ML
horizon_bars = 16
# 4h = 16 x 15m
target_type = binary
class_label_func = lambda x: 1 if x > 0.02 else 0
# Basic features for ML training (subset of all features)
features = close, ema10, ema20, ema50, adx14, rsi_now, macd_line, macd_sig, atr_pct, bb_width_pct
